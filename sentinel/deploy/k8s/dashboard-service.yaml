apiVersion: v1
kind: Service
metadata:
  name: sentinel-dashboard
  namespace: sentinel
spec:
  selector:
    app: sentinel-dashboard
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
